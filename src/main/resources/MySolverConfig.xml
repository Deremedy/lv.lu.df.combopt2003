<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">
    <environmentMode>REPRODUCIBLE</environmentMode>
    <!-- Define the model -->
    <solutionClass>my.combopt.domain.RouteSolution</solutionClass>
    <entityClass>my.combopt.domain.RouteStep</entityClass>

    <!-- Define the score function -->
    <scoreDirectorFactory>
        <constraintProviderClass>my.combopt.solver.RouteSolutionConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Configure the optimization algorithms (optional) -->
    <termination>
        <secondsSpentLimit>3</secondsSpentLimit>
    </termination>

    <constructionHeuristic />

    <localSearch>
        <unionMoveSelector>
            <changeMoveSelector>
                <valueSelector variableName="isActive"/>
            </changeMoveSelector>
            <swapMoveSelector>
                <variableNameIncludes>
                    <variableNameInclude>isActive</variableNameInclude>
                </variableNameIncludes>
            </swapMoveSelector>

            <subChainChangeMoveSelector>
                <subChainSelector>
                    <valueSelector variableName="nextStep"/>
                </subChainSelector>
                <valueSelector variableName="nextStep"/>
            </subChainChangeMoveSelector>

            <subChainSwapMoveSelector>
                <subChainSelector>
                    <valueSelector variableName="nextStep"/>
                </subChainSelector>
                <secondarySubChainSelector>
                    <valueSelector variableName="nextStep"/>
                </secondarySubChainSelector>
            </subChainSwapMoveSelector>
        </unionMoveSelector>
        <acceptor>
            <valueTabuSize>2</valueTabuSize>
        </acceptor>
        <forager>
            <acceptedCountLimit>1000</acceptedCountLimit>
        </forager>
    </localSearch>
</solver>